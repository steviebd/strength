{
  "name": "strength",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:expo": "infisical run --env=dev -- bun --filter @strength/expo dev",
    "dev:worker": "cd apps/worker && infisical export --env=dev --silent --format=dotenv --output-file=.dev.vars && bun run dev",
    "db:generate": "bun --filter @strength/db db:generate",
    "db:push:dev": "cd apps/worker && wrangler d1 migrations apply strength-db --local",
    "db:reset": "rm -rf apps/worker/.wrangler/ && bun run db:push:dev",
    "typecheck": "bun --filter '*' typecheck",
    "setup": "bun install",
    "check": "source ~/.vite-plus/env && vp check",
    "test": "source ~/.vite-plus/env && vp test run",
    "lint": "source ~/.vite-plus/env && vp lint",
    "fmt": "source ~/.vite-plus/env && vp fmt"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite-plus": "^0.1.18",
    "wrangler": "^4.83.0"
  },
  "packageManager": "bun@1.2.9"
}
