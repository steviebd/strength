{
  "name": "@strength/db",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "push": "drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0"
  }
}
